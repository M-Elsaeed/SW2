decomposition:

Bank branches: is a submodule of the online banking system
clients'services: is a submodule of the online banking system
client's accounts:is a submodule of Bank branches
pay services:is a sub module of client's services
transfers:is a sub module of client's services
bank account services:is a sub module of client's services
funds:is a sub module of transfers
bills:is a sub module of pay service
withdraw: is a sub module of bank account's services
deposit:is a sub module of bank account's services
cheque services :is a sub module of bank account's services
cash cheque:is a sub module of cheque services
issue cheque:is a sub module of cheque services


Uses:

clients uses client's services to make transactions

bank uses clients' accounts to get transctions' details 

Layers:

only bank and clients devices can use the main servers 

main servers use DBMS to complete customer's requests

Classes:
class diagram ( el howa el inheritance)